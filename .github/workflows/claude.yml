# Wires this repo (nantobv/.github) into the nantobv reusable Claude
# Code responder. Behavior, security gate, and SHA-pinned action versions
# live in claude-reusable.yml.
name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    uses: nantobv/.github/.github/workflows/claude-reusable.yml@main
    secrets: inherit
